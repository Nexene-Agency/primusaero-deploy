import SearchWithMap from "@components/map/search.with.map";
import React from "react";

const MapSearchClientPage = (props: any) => {
  return <SearchWithMap locale={props.locale} />;
};

export default MapSearchClientPage;
