import SearchWithMap2 from "@components/map/search.with.map.2";
import React from "react";

const MapSearchClientPage = (props: any) => {
  return <SearchWithMap2 locale={props.locale} />;
};

export default MapSearchClientPage;
